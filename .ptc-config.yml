# .ptc-config.yml — generated by `ptc-cli.sh init`
# Detected project kind: node
# Never commit an API token — provide it via the PTC_API_TOKEN environment variable.

source_locale: en

files:
  - file: locales/en.json
    output: locales/{{lang}}.json
